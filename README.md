# CIPS 2008 寿命预测软件

<p align="center">
  <strong>功率器件寿命预测与分析系统</strong><br>
  Power Device Lifetime Prediction & Analysis System
</p>

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.10+-blue.svg" alt="Python">
  <img src="https://img.shields.io/badge/React-18-blue.svg" alt="React">
  <img src="https://img.shields.io/badge/FastAPI-0.115-green.svg" alt="FastAPI">
  <img src="https://img.shields.io/badge/MUI-5.x-purple.svg" alt="MUI">
  <img src="https://img.shields.io/badge/License-MIT-yellow.svg" alt="License">
</p>

---

## 📖 项目简介

基于 **CIPS 2008 (Bayerer) 模型** 的功率器件寿命预测软件，支持多种寿命模型、雨流计数分析、Weibull可靠性评估等功能。本软件专为IGBT模块等功率半导体的可靠性工程而设计。

### 核心特性

- 🔬 **5种寿命模型**: Coffin-Manson, CM-Arrhenius, Norris-Landzberg, CIPS 2008, LESIT
- 📊 **雨流计数法**: ASTM E1049标准的循环提取算法
- 📈 **Weibull分析**: B10/B50/B63.2寿命计算
- 🎯 **敏感性分析**: 龙卷风图、热力图
- 📑 **报告导出**: PDF/Excel专业报告
- 🧪 **353个单元测试**: 全面覆盖核心算法

---

## 🚀 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+
- npm 或 yarn

### 安装步骤

#### 1. 克隆项目

```bash
git clone <repository-url>
cd IOL_PC_research
```

#### 2. 后端安装

```bash
cd backend

# 创建虚拟环境 (推荐)
python -m venv venv
source venv/bin/activate  # Linux/Mac
# 或
.\venv\Scripts\activate  # Windows

# 安装依赖
pip install -r requirements.txt

# 启动后端服务
python run.py
```

后端服务将在 http://localhost:8000 启动

#### 3. 前端安装

```bash
cd frontend

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

前端界面将在 http://localhost:5173 打开

---

## 📚 功能模块

### 1. 寿命预测计算

支持5种主流寿命预测模型：

| 模型 | 公式 | 适用场景 |
|------|------|----------|
| **Coffin-Manson** | `Nf = A × (ΔTj)^(-α)` | 基础热疲劳 |
| **CM-Arrhenius** | `Nf = A × (ΔTj)^(-α) × exp(Ea/(kB×Tj))` | 温度依赖 |
| **Norris-Landzberg** | `Nf = A × (ΔTj)^(-α) × f^β × exp(Ea/(kB×Tj_max))` | 含频率因子 |
| **CIPS 2008** | `Nf = K × (ΔTj)^β1 × exp(β2/Tj_max) × t_on^β3 × I^β4 × V^β5 × D^β6` | **推荐** |
| **LESIT** | `Nf = A × (ΔTj)^α × exp(Q/(R×Tj_min))` | 最低温度影响 |

### 2. 雨流计数法 (Rainflow Counting)

基于 **ASTM E1049** 标准实现：
- 从不规则温度/载荷历程中提取循环
- 生成循环矩阵
- 直方图可视化

### 3. 累计损伤计算 (Miner法则)

- 任务剖面定义
- 线性损伤累积
- 临界损伤预警 (D ≥ 1.0)

### 4. Weibull可靠性分析

- 失效数据拟合
- B10/B50/B63.2 特征寿命
- 可靠度曲线

### 5. 敏感性分析

- 单参数敏感性 (龙卷风图)
- 双参数交叉敏感性 (热力图)
- 参数优化建议

### 6. 参数拟合

- 基于试验数据的非线性最小二乘拟合
- R²拟合优度评估
- CIPS 2008专用拟合

### 7. 剩余寿命评估

- 当前退化状态输入
- 退化趋势外推
- 健康指数计算

### 8. 安全裕度计算

- 设计寿命 vs 预测寿命
- 安全系数计算
- 统计不确定性分析

---

## 🔌 API文档

### 基础URL
```
http://localhost:8000/api
```

### 主要端点

#### 寿命预测

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/prediction/calculate` | 单模型寿命预测 |
| POST | `/prediction/compare` | 多模型对比 |
| POST | `/prediction/sensitivity` | 敏感性分析 |
| GET | `/prediction/models` | 可用模型列表 |

#### 雨流计数

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/rainflow/count` | 雨流循环提取 |
| POST | `/rainflow/matrix` | 循环矩阵生成 |
| POST | `/rainflow/histogram` | 直方图数据 |

#### 损伤分析

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/damage/calculate` | Miner损伤计算 |
| POST | `/damage/remaining-life` | 剩余寿命评估 |
| POST | `/damage/safety-margin/calculate` | 安全裕度计算 |

#### Weibull分析

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/analysis/weibull/fit` | Weibull拟合 |
| POST | `/analysis/weibull/b-life` | B寿命计算 |
| POST | `/analysis/weibull/probability-plot` | 概率图数据 |

#### 数据导出

| 方法 | 端点 | 描述 |
|------|------|------|
| POST | `/export/csv/predictions` | 预测结果CSV |
| POST | `/export/report/lifetime-prediction` | PDF报告 |
| POST | `/export/excel/data` | Excel导出 |

### 示例请求

#### 寿命预测

```bash
curl -X POST http://localhost:8000/api/prediction/calculate \
  -H "Content-Type: application/json" \
  -d '{
    "model_type": "cips-2008",
    "parameters": {
      "delta_Tj": 80,
      "Tj_max": 398,
      "t_on": 1.0,
      "I": 100,
      "V": 1200,
      "D": 300
    },
    "safety_factor": 1.0
  }'
```

#### 雨流计数

```bash
curl -X POST http://localhost:8000/api/rainflow/count \
  -H "Content-Type: application/json" \
  -d '{
    "data_points": [
      {"time": 0, "value": 25},
      {"time": 1, "value": 85},
      {"time": 2, "value": 35},
      {"time": 3, "value": 95},
      {"time": 4, "value": 45}
    ],
    "bin_count": 64
  }'
```

---

## 🏗️ 项目结构

```
IOL_PC_research/
├── backend/                    # FastAPI后端
│   ├── app/
│   │   ├── core/              # 核心算法
│   │   │   ├── models/        # 寿命模型
│   │   │   │   ├── cips_2008.py
│   │   │   │   ├── coffin_manson.py
│   │   │   │   ├── norris_landzberg.py
│   │   │   │   ├── lesit.py
│   │   │   │   └── model_factory.py
│   │   │   ├── rainflow.py    # 雨流计数
│   │   │   ├── damage_accumulation.py
│   │   │   ├── weibull.py
│   │   │   ├── sensitivity.py
│   │   │   ├── fitting.py
│   │   │   └── export/        # 导出模块
│   │   │       ├── pdf_generator.py
│   │   │       └── excel_generator.py
│   │   ├── api/               # API端点
│   │   ├── models/            # SQLAlchemy模型
│   │   ├── schemas/           # Pydantic模式
│   │   └── db/                # 数据库
│   ├── tests/                 # 单元测试 (353用例)
│   ├── requirements.txt
│   └── run.py
│
├── frontend/                   # React前端
│   ├── src/
│   │   ├── components/        # React组件
│   │   │   ├── Prediction/
│   │   │   ├── Rainflow/
│   │   │   ├── DamageAccumulation/
│   │   │   ├── RemainingLife/
│   │   │   ├── Visualization/
│   │   │   └── ErrorBoundary/
│   │   ├── pages/             # 页面
│   │   ├── services/          # API服务
│   │   ├── stores/            # Zustand状态
│   │   └── types/             # TypeScript类型
│   ├── package.json
│   └── vite.config.ts
│
└── README.md
```

---

## 🧪 测试

### 运行后端测试

```bash
cd backend

# 运行所有测试
pytest tests/ -v

# 运行特定模块测试
pytest tests/test_rainflow.py -v
pytest tests/test_damage_accumulation.py -v

# 生成覆盖率报告
pytest tests/ --cov=app --cov-report=html
```

### 测试覆盖

| 模块 | 测试文件 | 用例数 |
|------|----------|--------|
| 雨流计数 | `test_rainflow.py` | 45 |
| 损伤累积 | `test_damage_accumulation.py` | 38 |
| Weibull | `test_weibull.py` | 52 |
| 敏感性 | `test_sensitivity.py` | 65 |
| 参数拟合 | `test_fitting.py` | 48 |
| 其他 | - | 105 |
| **总计** | - | **353** |

---

## 📦 技术栈

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.10+ | 核心语言 |
| FastAPI | 0.115 | Web框架 |
| SQLAlchemy | 2.0 | ORM |
| NumPy | 2.1 | 数值计算 |
| SciPy | 1.14 | 科学计算 |
| scikit-learn | 1.5 | 机器学习 |
| reportlab | 4.2 | PDF生成 |
| openpyxl | 3.1 | Excel处理 |

### 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18.2 | UI框架 |
| TypeScript | 5.2 | 类型系统 |
| MUI | 5.15 | UI组件库 |
| ECharts | 5.4 | 图表库 |
| Zustand | 4.4 | 状态管理 |
| Axios | 1.6 | HTTP客户端 |
| Vite | 5.0 | 构建工具 |

---

## 📐 架构设计

### 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                    前端 (React + TypeScript + MUI)              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 寿命预测  │ │ 雨流计数 │ │ 损伤分析  │ │ Weibull  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │ 敏感性   │ │ 剩余寿命 │ │ 安全裕度 │ │ 报告导出 │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
└───────────────────────────┬─────────────────────────────────────┘
                            │ REST API (JSON)
┌───────────────────────────▼─────────────────────────────────────┐
│                    后端 (FastAPI + Python)                       │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ 寿命模型引擎 │ │ 雨流计数引擎 │ │ 损伤累积引擎 │               │
│  │ (5种模型)   │ │ (ASTM E1049)│ │ (Miner法则) │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐               │
│  │ Weibull引擎 │ │ 敏感性分析  │ │ 导出引擎     │               │
│  └─────────────┘ └─────────────┘ └─────────────┘               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────────┐
│                    数据层 (SQLite + 本地文件)                    │
└─────────────────────────────────────────────────────────────────┘
```

### 设计模式

- **工厂模式**: `ModelFactory` 用于创建不同寿命模型
- **策略模式**: 不同寿命模型实现统一接口
- **仓储模式**: `CRUDBase` 提供通用数据访问
- **依赖注入**: FastAPI的`Depends`机制

---

## 🔧 开发指南

### 添加新的寿命模型

1. 在 `backend/app/core/models/` 创建新模型文件:

```python
from app.core.models.model_base import LifetimeModelBase

class NewModel(LifetimeModelBase):
    def get_model_name(self) -> str:
        return "New Model"

    def calculate_cycles_to_failure(self, **params) -> float:
        # 实现计算逻辑
        pass
```

2. 在 `model_factory.py` 注册模型:

```python
ModelFactory.register_model("new-model", NewModel)
```

### 添加新的API端点

1. 在 `backend/app/api/` 创建或修改路由文件
2. 在 `main.py` 中注册路由
3. 添加对应的Pydantic schema

### 代码规范

- Python: 遵循 PEP 8
- TypeScript: ESLint + Prettier
- 提交信息: Conventional Commits

---

## 📖 参考文献

1. Bayerer, R., et al. "Model for Power Cycling Lifetime of IGBT Modules - various factors influencing lifetime", CIPS 2008
2. ASTM E1049-85(2017), "Standard Practices for Cycle Counting in Fatigue Analysis"
3. Miner, M.A. "Cumulative Damage in Fatigue", Journal of Applied Mechanics, 1945

---

## 📄 License

MIT License

---

## 👥 贡献

欢迎提交 Issue 和 Pull Request。

---

<p align="center">
  Made with ❤️ for Power Electronics Reliability Engineering
</p>
